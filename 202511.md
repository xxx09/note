# 生成md表格
不同方法的对比
## 方法一：手动编写
| 算法         | 数据 | 网络架构 | 功能 |
|------       |------|------|------|
| Exbody2     | Feasibility-Diversity Principle, <br> easy2hard dataset filtering(error threshold), <br> expressive standing postures, walking at various speeds and patterns, squatting, and dancing| generalist policy-(finetune)>specialist policy; <br> twostage teacher(pt, ot-n->ot,gt)-student(ot-n->ot,gt), DAgger(roll out stu policy )   | finetune more robust; <br> moiton-velocity decoupled control strategy 2024.12|
| GMT     | Adaptive sampling; random clip, track performance-based probability; data curation [rule based + policy completion]；<br> local key body position, consecutive frames| Moe ; two-stage, teacher(ot,et, gt, gt->gt+200) + student (ot-20->ot, gt->gt+200, gt)DAgger; 100 frames convolution network-> Zt128| 2025.6 <br> 暂未开源训练代码 <br> 部分可观，硬件限制，数据分布，模型表现力；<br> RTX4090, 4096env, teacher 3 days, student 1 day|
| TWIST    | Large internet data 15k clip(AMASS, OMOMO), <br> small mocap data 150clip(noise, less stable), 3d pos and ori | two stage, tea(ot, gt->gt+20):RL, stu(ot-10->ot, gt):BC+RL,   | 2025.5 |
| TWIST2    | 赵六 | 32   | 深圳 |

# 20251105
- 1. 诺亦腾惯补
惯补：12.5w+手套=15w
maners手套：位姿，数采员手做建模； 跟xsense集合更方便；
光惯
数据采购直接买数据；手部精细操作、VLA、r
可以建议买一套xsense; 光学隔一段时间要重新校准，标定相机；

- 2. 论文阅读

- 3. kuailechongbai_v4.npz似乎没有问题；

# 20251106
- 1. GMR 增加vis可视化交互功能
- 2. GMR 增加数据处理功能，增加高度；
- 3. 提取fallAndGetUp1_subject1 起身数据；1950-2075: 站立到躺倒，2075-2216： 从躺倒到站起来【向左翻身撑起来】； getup1.pkl
第一帧数据： pos[0, 0, 0.09], quat[0.7, -0.18, -0.66, -0.17]
切到getup分支，修改相应的训练超参； 接触惩罚设置为0；
8192 初始训练return起不来
- 4. 改为4096，增大一些误差范围，terminate的阈值，增大外力扰动的时间间隔；【return 起不来】
在修改外力扰动的大小；扩大terminate的范围；【return能起来，但趴在地上不动；】
将terminate改回去；修改外力扰动的值，

- 5. 训练g1，看看能否起来；
g1有个数据维度的问题，
# 临时禁用有问题的关节参数随机化以避免形状不匹配错误
        self.events.rand_joint_parameter = None

为什么mirax3没有这个问题？【等待查验】
【应该是配置出现了问题，tracking_env_cfg中EventCfg中的add_base_mass中body_name有不存在的base_link, 导致配置没正常进行；重新配置之后即正常运行】

# 20251107
- 1. 训练g1，return后续能起来，且ok；但mirax3训练起不来，仔细查看发现mirax3手掌没有collision；
- 2. 添加手掌，训练mirax3，看看能否起来；【起不来】
- 3. action_scale*2， 查看能否起来；似乎ok了‘’
后续将推力改为以前的大小；查看是否ok；

# 20251111
- 1. sim2sim有问题，g1sim2sim可以，mirax3sim2sim不能起身；
但 mirax3在真机上测试直接能够成功；【相对比较稳定】
仔细检查sim2sim，尤其是碰撞体的设置；
检查发现mirax3碰撞体没问题，但是关节的effort限制设置有问题；改为mirax3.py中的值之后即可恢复正常；



